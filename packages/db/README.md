# @dashboard-divisi/db — DEPRECATED

> **Jangan edit.** Paket ini peninggalan era Prisma (NestJS).
> Sumber kebenaran skema sekarang = `apps/api/database/migrations` (Laravel 13).
> File `prisma/schema.prisma`, `prisma/seed.ts`, dan migrations di sini duplikat dan tidak dipakai CI lagi (CI 2026-09-01 sudah pindah ke `php artisan migrate`).

Disimpan hanya untuk arsip; akan dihapus setelah `OPS-07` staging stabil.
